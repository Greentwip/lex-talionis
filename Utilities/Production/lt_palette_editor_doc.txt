Main Editor
    MainView
        image
        screen_scale
        *set_image(ImageMap, Palette)
        *clear_scene()
        *update()
        *mousePressEvent()
        *wheelEvent()

    Palette List
        Title Label
        list of PaletteFrames
            name
            *set_name
            PaletteDisplay
                list of colors (QtColor)
                list of ColorDisplays
                    *set_color(QtColor)
                    *mousePress
                *set_color(idx, QtColor)
            *set_color(idx, QtColor)
            copy Button
            *get_palette_from_image(image_name)
        current_index
        Radio Button Group
        *add_palette_from_image(idx, image_name)
        *duplicate(idx)
        *set_current_palette(idx)
        *get_current_palette()
        *get_palette(idx)

    ImageMap List
        list of ImageMaps
            weapon_name
            grid
            *get(x, y)
        current_index
        *get_current_map()
        *set_current_map(weapon_name)
        *get_available_weapons()

    Class Text Box (editable bool)
    Weapon Text Box (editable bool) -> Also a combobox so you can select from available weapons
    Palette Text Box (editable bool)
    View Animation Button (editable bool)

    *load_image() -> all editable, no view animation button
    *load_weapon_animation() -> Class and Weapon set, palette editable, animation possible
    *load_class_animation() -> Class set, Weapon combobox, palette editable, animation possible
    *save() -> Creates the images that are needed

    *update_view() -> calls view.set image with imagemap_list.get_current_map and palette_list.get_current_palette
